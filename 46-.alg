algoritmo "semnome"
var
cont, soma: inteiro
inicio
cont <- 4
soma <- 0
     enquanto cont < 100 faca
       cont <- cont + 2
       soma <- soma + cont
       escreval (cont, ", ")
     fimenquanto
escreval (" ")
escreval ("A soma de todos esses números vale: ", soma)
fimalgoritmo
